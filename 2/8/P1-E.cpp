з#include <stdio.h>

int main(){
    int n, c = 0;
    int array[100];
    scanf("%d", &n);
    for (int i = 0; i < n; ++i) scanf("%d", &array[i]);
    for (int i = 1; i < n; ++i) if () ++c;
    printf("%d", c);
}
