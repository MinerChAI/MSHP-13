#include <iostream>
#include <iomanip>

int main(){
    int n, c = 0, sum, num, cur;
    std::cin >> n;
    for(;n;--n, sum += cur, ++num){
        std::cin >> cur;
        if (cur > 0) ++c;
    }
    std::cout << 
}
